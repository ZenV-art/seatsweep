NEXT_PUBLIC_APP_URL=https://seatsweep.com

# Stripe (optional — checkout + portal routes degrade gracefully without keys)
STRIPE_SECRET_KEY=sk_test_xxx
STRIPE_PRICE_TEAM=price_xxx
STRIPE_PRICE_BUSINESS=price_xxx
STRIPE_TEST_CUSTOMER_ID=cus_xxx

# Supabase (reserved for when auth/DB are wired)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Slack (required to run a real Slack ghost-seat scan)
# Create a Slack app at https://api.slack.com/apps with scopes:
#   users:read, users:read.email
# Install to workspace, then copy the Bot User OAuth Token (starts with xoxb-).
SLACK_BOT_TOKEN=
